export db locale
deploy (supabase ?? )
riepilogo: READ
testing
batch movimenti futuri -> ogni giorno a 00:00 (se c'è un movimento nella tabella dei futuri deve passarlo negli attuali e fare la TRANSAZIONE di INSERIMENTO tra CONTI e CATEGORIE corrispondente)
auth?